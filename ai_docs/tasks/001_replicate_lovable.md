# Replicate Lovable – Task Ledger

## Goals
- Clone the "lovable" Vite application into this Next.js + Supabase starter.
- Harvest its design system and progressively recreate its pages within this repo.
- Keep the reference project isolated in a temporary Git submodule.
- Maintain a clear, step-by-step ledger so the developer can easily track progress.

## Task Backlog
- [x] 1. Obtain & mount reference code as a Git submodule.
- [x] 2. Inspect the submodule and report the core pages discovered.
- [x] 3. Port the Tailwind colour palette into `app/globals.css`.
- [ ] 4. Recreate the landing page structure and port its components (`Navbar`, `HeroSection`, etc.).
- [ ] 5. Generate page-specific task files for every remaining page in the reference app.
- [ ] 6. Implement each page according to its task file and ensure all acceptance criteria pass.

## Completed Tasks
- 1. Obtain & mount reference code as a Git submodule.
- 2. Inspect the submodule and report the core pages discovered.
- 3. Port the Tailwind colour palette into `app/globals.css`.

## Next Task
**Recreate the landing page structure and port its components (`Navbar`, `HeroSection`, etc.)** 
